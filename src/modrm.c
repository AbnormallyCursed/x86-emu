#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#include "x86_types.h"

static inline void modrmWrite(bit64 reg[8], gooblyram *ram, register bit64 RIP) {

}

static inline bit64 modrmRead(bit64 reg[8], gooblyram *ram, register bit64 RIP) {

}